import arb.Integer;
import arb.Real;
import arb.functions.Function;

public class factorℝ1 implements Function<Integer, Real> {
   public Integer n;

   public Real evaluate(Integer in, int order, int bits, Real result) {
      return x.add(in, bits, result);
   }
}
