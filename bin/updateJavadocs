#!/bin/sh -x

FMDIR=$(dirname $(dirname $(realpath $0)))

cd $FMDIR

mvn javadoc:javadoc

cp -vr target/site/apidocs/. ../arb4j-pages/

cd ../arb4j-pages

git add .

git commit -m 'update javadocs'

git push
