* incomplete renders not being deleted if render is cancelled before
complete

* lock and unlock using mprotect doesnt quite work..

* memory leak in ComplexFunctionRenderer or AnimatedComplexFunctionSequencer
