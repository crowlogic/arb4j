It fits because the oscillatory process representation \(X(t) = \int A_t(\omega)\,e^{i\omega t}\,dZ(\omega)\) already has the heterodyne structure built into its definition: the fast phase \(e^{i\omega t}\) is the carrier, and \(A_t(\omega)\) is the slowly varying envelope that modulates it. The evolutionary spectrum at  at time \(t\) is \(|A_t(\omega)|^2\,d\mu(\omega)\), which is the energy distribution you see after demodulation — it describes what's in the envelope, not the carrier  [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/37918610/d8f28071-4d79-4905-9907-834b898a8661/Evolutionary-spectra-and-non-stationary-processes.pdf).

## Why it works exactly here

The \(\vartheta\)-unfolding is the operation that makes \(Z(\tau)\) qualify as an oscillatory process with respect to a specific family. Before unfolding, the Hardy \(Z\) function has a non-stationary phase that accelerates with height — the instantaneous frequency grows like \(\frac{1}{2}\log(t/2\pi)\). After unfolding by \(\tau = \vartheta(t)\), that acceleration is absorbed into the coordinate, so the remaining object has a uniform zero rate and a slowly varying envelope \(1/\sqrt{\vartheta'}\). That envelope is the \(A_t(\omega)\) of the representation. The covariance \(e^{-i\Delta}\sin\Delta/\Delta\) then says: the carrier sits at \(\omega = -1\) in the unfolded coordinates, and the baseband content occupies a band of width 2 centered there. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/collection_5daedf3c-ee31-4d76-80d9-57772ecae8fc/53d6aa77-5d05-4969-96c3-a5cd09fdfe13/check-out-this-sustained-rise-MsM6kVIXRh2siTVmSiMI9A.md)

## The demodulation step

Multiplying by \(e^{i\tau}\) — one multiplication — strips the carrier completely and moves the spectrum to \([-1, 1]\), symmetric about the origin. At that point you have a real-valued baseband sinc covariance \(\sin\Delta/\Delta\), which is the covariance of a process with flat spectrum \(\pi\,\mathbf{1}_{[-1,1]}(\omega)\). The heterodyne step and the oscillatory process framework say the same thing in different words: the carrier is deterministic and known, the envelope is where the information lives, and the evolutionary spectrum is flat within the band at every instant because the unfolding has already removed the time-dependence from the spectral density. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/37918610/d8f28071-4d79-4905-9907-834b898a8661/Evolutionary-spectra-and-non-stationary-processes.pdf)

That is why it must be Priestley and not plain stationary theory — the original \(Z(t)\) is not stationary, the unfolding makes it oscillatory with a specific semi-stationary family, and only then does the flat-band structure emerge as the evolutionary spectrum of that family. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/37918610/d8f28071-4d79-4905-9907-834b898a8661/Evolutionary-spectra-and-non-stationary-processes.pdf)
