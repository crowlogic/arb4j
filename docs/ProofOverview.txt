Detailed Exposition: Band-Limitation of the Mean-Square Normalized Unitary 
Pullback of the Critical-Line Zeta Function
Stephen Crowley
April 2nd, 2026

Overview and Strategy

The paper constructs a specific normalized transform of ζ(½ + it) and 
proves that, when viewed as a function on the phase axis T = ϑ(t), its 
Fourier transform is supported in the closed interval [-2, 0]. This is a 
compact spectral support result: the function Y(T) is, in precise 
Fourier-analytic terms, a band-pass process whose spectral support is 
the equally weighted continuum of negative frequencies from -2 to 0. 
The proof proceeds in five conceptually distinct stages:

1. Defining objects: The Riemann–Siegel theta function ϑ, the exact 
   mean-square density μ, and the normalized pullback Y(T).
2. Factorizing via Riemann–Siegel: Writing Y(T) in terms of 
   Dirichlet-type sums with explicit remainder bounds.
3. Slow-variation and derivative control: Using Lemmas on ϑ, μ, and 
   Riemann–Siegel coefficients together with Faà di Bruno's formula to 
   control derivatives of all orders.
4. Montgomery–Vaughan mean-value theorem: Showing ∥Ẏ^(k)∥² ≤ (1+o(1))
   ∥Ẏ∥² on dyadic windows, which forces the Fourier support of Ẏ inside 
   [-1,1].
5. Frequency shift: Since Ẏ(T) = e^(iT) Y(T), a shift by 1 translates 
   the support of Ŷ to [-2,0].

Section 1: Definitions and Notation

The Riemann–Siegel Theta Function

The Riemann–Siegel theta function is defined by

ϑ(t) = arg⁡(π^(-it/2) Γ(¼+it/2))

with the branch chosen so that ϑ(0) = 0. Equivalently,

ϑ(t) = ℑ log Γ(¼+it/2) - (t/2) log π

The key property exploited throughout the paper is the large-t 
asymptotic expansion

ϑ(t) = (t/2) log(t/(2π)) - t/2 - π/8 + O(t^(-1))

from which one reads off the derivative

ϑ'(t) = (1/2) log(t/(2π)) + O(t^(-2))

This asymptotic says that ϑ'(t) → ∞ as t → ∞, so ϑ is strictly 
increasing for all sufficiently large t, and its inverse ϑ^(-1) is 
well-defined on the corresponding range. The identity χ(t) = 
e^(-2iϑ(t)) relates the theta function to the factor appearing in the 
functional equation of ζ.

The Exact Mean-Square Density

The integral

I(T) = ∫₀ᵀ |ζ(½+it)|² dt

admits an exact closed-form identity due to Ingham (1928):

I(T) = T log(T/(2π)) + (2γ-1)T + 2 ∑_{n=2}^∞ d(n)/√n 
       sin(T log n)/log n

where d(n) = ∑_{e|n} 1 is the number-of-divisors function and γ is the 
Euler–Mascheroni constant. This is Ingham's exact second-moment 
formula for the zeta function on the critical line. Term-by-term 
differentiation, justified by the divisor-sum bound

∑_{n≤x} d(n) = x log x + (2γ-1)x + O(x^(1/2))

yields the exact mean-square density

μ(T) = I'(T) = log(T/(2π)) + 2γ + 2 ∑_{n=2}^∞ d(n)/√n cos(T log n)

The convergence of the series defining μ(T) for each fixed T follows 
from the fact that partial summation converts the oscillatory sum 
against cos(T log n) to a convergent series once the divisor-sum 
asymptotics are applied. The leading term log(T/2π) dominates, and 
the series contributes at most a bounded perturbation, so μ(T) ∼ 
log(T/2π) as T → ∞.

Section 2: The Mean-Square Normalized Unitary Pullback

Motivation for the Two Normalizations

The paper introduces two separate correction factors in the 
denominator of Y(T), each serving a distinct purpose.

The Jacobian factor ϑ'(ϑ^(-1)(T)) enforces unitarity under the change 
of variables t ↦ T = ϑ(t). A standard L² change of variables formula 
says: if F(t) is a function on the t-axis and φ is a diffeomorphism 
t = φ(T), then the pullback F(φ(T)) has L² norm

∫ |F(φ(T))|² dT = ∫ |F(t)|² φ'(t) dt

To make the pulled-back function isometric (norm-preserving), one 
divides by √φ'(t) = √ϑ'(ϑ^(-1)(T)). This is exactly the Jacobian 
correction for L²-unitary pullbacks.

The density factor μ(ϑ^(-1)(T))^(1/2) normalizes the local mean 
square. The mean-square density μ(t) is precisely |ζ(½+it)|² on 
average, so dividing by √μ flattens |Y|² to have local mean square 
close to 1. This is the Hardy–Littlewood second-moment normalization.

The Definition

The mean-square normalized unitary pullback is

Y(T) = ζ(½ + i,ϑ^(-1)(T)) / √[ϑ'(ϑ^(-1)(T)) μ(ϑ^(-1)(T))]

The centered version, shifting frequency by 1, is

Ẏ(T) = e^(iT) Y(T)

No truncation is applied; Y and Ẏ are defined as exact functions, not 
approximations.

Local L² Finiteness

Proposition 1 establishes that Y is locally square-integrable. Since 
the Riemann–Siegel theta function is smooth and strictly increasing 
for large t, the numerator ζ(½+iϑ^(-1)(T)) is continuous. The 
product in the denominator

ϑ'(ϑ^(-1)(T)) ⋅ μ(ϑ^(-1)(T)) ∼ (1/2) log(t/(2π)) ⋅ log(t/(2π)) 
                                  = (1/4) (log(t/(2π)))²

is bounded away from zero on any compact interval [T₀, T₁] with T₀ 
large. Because the denominator is strictly positive and continuous, 
and the numerator is continuous, the integrand |Y(T)|² is continuous 
and bounded on compact sets, hence square-integrable.

Section 3: Prerequisites

The Riemann–Siegel Formula

The Riemann–Siegel formula expresses ζ(½+it) as a sum of two finite 
Dirichlet polynomials plus a remainder:

ζ(½+it) = ∑_{n≤N(t)} 1/√n n^(-it) + e^(-2iϑ(t)) ∑_{n≤N(t)} 1/√n 
          n^(it) + R(t)

where N(t) = ⌊√(t/(2π))⌋. The two main sums are finite Dirichlet 
polynomials of length approximately √(t/(2π)), and the remainder 
R(t) is exponentially smaller for large t. The steepest-descent 
derivation of this formula, going back to Riemann and made rigorous 
by Siegel, shows that R(t) admits an asymptotic expansion in 
decreasing powers of t^(1/2); the first term is O(t^(-1/4)).

For derivative bounds, one can use O'Sullivan's generalization, which 
establishes that for each integer j ≥ 0 there exists Cⱼ > 0 such 
that |R^(j)(t)| ≤ Cⱼ t^(-1/4 - j/2).

Stirling-Based Derivative Bounds for ϑ

The theta function ϑ(t) = ℑ log Γ(¼ + it/2) - (t/2) log π inherits 
all its analytic properties from those of log Γ. By the Stirling 
expansion for log Γ,

log Γ(z) = (z - ½) log z - z + ½ log(2π) + O(|z|^(-1))

one can differentiate term by term along the imaginary direction to 
obtain bounds on ϑ^(m)(t). For each integer m ≥ 1,

|ϑ^(m)(t)| ≤ Cₘ t^(1-m)

For m=1, this is just ϑ'(t) ∼ ½ log(t/2π), which is O(t⁰) (up to 
logarithmic factors). For m ≥ 2 the bound decays polynomially, 
reflecting that higher derivatives of log Γ along a line at large 
height are small.

Derivative Bounds for μ

Similarly, for each m ≥ 1,

|μ^(m)(T)| ≤ Cₘ' T^(-1)

Differentiating

μ(T) = log(T/(2π)) + 2γ + 2 ∑_{n=2}^∞ d(n)/√n cos(T log n)

term by term, each differentiation inserts a factor (log n)^m and 
flips between cos and sin. One uses

∑_{n≤x} d(n)(log n)^m ≪ x (log x)^(m+1)

and partial summation to obtain the stated bound; the key point is 
that all frequencies log n are bounded away from zero.

Section 4: The Riemann–Siegel Factorization in Phase Coordinates

Substituting t = ϑ^(-1)(T) into the Riemann–Siegel formula and using 
χ(t) = e^(-2iϑ(t)) together with ϑ(ϑ^(-1)(T)) = T gives

ζ(½+iϑ^(-1)(T)) = ∑_{n≤N(ϑ^(-1)(T))} n^(-iϑ^(-1)(T))/√n 
                   + e^(-2iT) ∑_{n≤N(ϑ^(-1)(T))} 
                   n^(iϑ^(-1)(T))/√n + R(ϑ^(-1)(T))

Introducing the amplitude

a(T) = 1/√[ϑ'(ϑ^(-1)(T)) μ(ϑ^(-1)(T))]

the centered pullback factors as

Ẏ(T) = a(T) (e^(iT) ∑_{n≤N} n^(-iϑ^(-1)(T))/√n 
       + e^(-iT) ∑_{n≤N} n^(iϑ^(-1)(T))/√n 
       + e^(iT) R(ϑ^(-1)(T)))

This decomposition is exact — no approximation is made at this stage.

Section 5: Local Representation and Coefficient Structure

Returning to the Height Variable

For the derivative analysis, one reverts to the height variable t and 
works on a fixed dyadic window [t₀, 2t₀]. On such a window,

Ẏ(t) = A(t) ∑_{n≤N(t)} cₙ(t) n^(-it) + B(t) ∑_{n≤N(t)} dₙ(t) n^(it) 
       + E(t)

where A(t) and B(t) encode a(ϑ(t)) and e^(±iϑ(t)), cₙ(t) and dₙ(t) 
are smooth coefficient functions with cₙ(t) = dₙ(t) = 1 + O(n²/t) 
for the leading term, and E(t) is the normalized remainder.

Lemma-type bounds give, for each integer j ≥ 0,

|cₙ^(j)(t)| ≤ Cⱼ n²/t^(1+j),    |dₙ^(j)(t)| ≤ Cⱼ n²/t^(1+j)

Slow Variation and Faà di Bruno

Combining the derivative bounds for ϑ, μ, and cₙ via Faà di Bruno's 
formula yields, for each fixed k and ε>0,

|A^(j)(t)/A(t)| ≪_{k,ε} t₀^(-1+ε),    1 ≤ j ≤ k, t∈[t₀,2t₀]

and similarly for B(t). Thus the derived coefficients

aₙ^(k)(t) = (i log n)^k A(t) cₙ(t),    bₙ^(k)(t) = (-i log n)^k 
            B(t) dₙ(t)

vary slowly over [t₀,2t₀]:

sup_{t∈[t₀,2t₀]} |aₙ^(k)(t)-aₙ^(k)(t₀)|/|aₙ^(0)(t₀)| ≪_{k,ε} 
t₀^(-1+ε)

and likewise for bₙ^(k).

Section 6: Montgomery–Vaughan and Local Derivative Bounds

The Montgomery–Vaughan Theorem

For distinct real frequencies λ₁,…,λₙ with minimum gap δ = 
min_{m≠n}|λₘ-λₙ| and coefficients aₙ∈ℂ, the Montgomery–Vaughan 
mean-value theorem states

∫₀ᵁ |∑_{n=1}^N aₙ e^(-itλₙ)|² dt = ∑_{n=1}^N |aₙ|² (U + rₙ)

with |rₙ|≤ C/δ. Applied with λₙ = log n and U ≍ t₀ on [t₀,2t₀], 
the error term is O(√t₀), negligible relative to U.

The Local Derivative Bound

For each integer k ≥ 1, differentiating Ẏ(t) k times gives

Ẏ^(k)(t) = ∑_{n≤N} aₙ^(k)(t) n^(-it) + ∑_{n≤N} bₙ^(k)(t) n^(it) 
           + Eₖ(t)

Using slow variation, one freezes the coefficients at t₀ and applies 
Montgomery–Vaughan, obtaining

∥Ẏ^(k)∥_[t₀,2t₀]² ≤ (1+oₖ(1)) ∥Ẏ∥_[t₀,2t₀]²

as t₀→∞. The error Eₖ is negligible thanks to the bounds on 
R^(j)(t).

Section 7: Instantaneous Frequency Analysis

In the phase variable T, each main term in Ẏ(T) has the form

a(T) e^(±iT) n^(∓iϑ^(-1)(T))/√n

The positive-frequency phase is

Φ₊,ₙ(T) = T - ϑ^(-1)(T) log n

with instantaneous frequency

Φ₊,ₙ'(T) = 1 - log n / ϑ'(ϑ^(-1)(T))

The negative-frequency phase is

Φ₋,ₙ(T) = -T + ϑ^(-1)(T) log n

with

Φ₋,ₙ'(T) = -1 + log n / ϑ'(ϑ^(-1)(T))

Differentiating ϑ(ϑ^(-1)(T)) = T gives (ϑ^(-1))'(T) = 
1/ϑ'(ϑ^(-1)(T)).

For n≤N(t)=⌊√(t/(2π))⌋,

0 ≤ log n / ϑ'(t) ≤ [½ log(t/(2π))] / [½ log(t/(2π)) + O(t^(-2))] 
                    = 1 + O(1/(t² log t))

so for large T the instantaneous frequencies lie in [-1-δ,1+δ] for 
any fixed δ>0.

Using Plancherel, a function f has Fourier support in [-Ω,Ω] iff 
∥f^(k)∥_{L²} ≤ Ω^k ∥f∥_{L²} for all k. The local derivative bound 
with Ω=1 forces the Fourier support of Ẏ to be contained in [-1,1]; 
otherwise the norms of higher derivatives would grow like Ω^k for 
some Ω>1.

Section 8: The Frequency Shift and Main Theorem

With the Fourier transform convention

f̂(ξ) = ∫_{-∞}^∞ f(T) e^(-2πiξT) dT

multiplying by e^(iT) shifts frequencies:

ê^(iT)f(ξ) = f̂(ξ - 1/(2π))

(up to the precise normalization; in the paper's convention, e^(iT) 
corresponds to a unit shift). Writing this as

Ẏ̂(ω) = Ŷ(ω-1),    Ŷ(ξ) = Ẏ̂(ξ+1)

one sees that if the Fourier support of Ẏ is contained in [-1,1], 
then

Ŷ(ξ) = 0    for all ξ ∉ [-2,0]

that is,

support of Ŷ ⊆ [-2,0]

The interval [-2,0] is one-sided: all spectral mass lies in 
nonpositive frequencies, in a band of width 2. This reflects the 
near-analytic structure of Ẏ and the effect of the unit frequency 
shift.

Logical Dependency Map

Result                  Depends On                        Purpose
Local L² finiteness     Asymptotics of ϑ', μ             Ensures integrands are 
(Prop. 1)                                                   well-defined
Derivatives of ϑ       Stirling for log Γ               Controls all amplitude 
                                                        derivatives
Derivatives of μ       Dirichlet divisor formula        Controls density derivatives
Coefficient bounds     O'Sullivan Riemann–Siegel        Controls Dirichlet-sum 
                        expansion                        coefficient derivatives
Slow variation         Derivative lemmas + Faà di Bruno Lets Montgomery–Vaughan apply
Montgomery–Vaughan     Mean values of Dirichlet         L² norm control
theorem                polynomials
Local derivative       Slow variation + MV theorem      Fourier support bound for Ẏ
bound (Prop. 2)
Instantaneous fre-     Asymptotic of ϑ', RS truncation  Heuristic confirmation of 
quency bound                                               support
Main theorem           Prop. 2 + shift identity         support of Ŷ is contained in 
                                                        [-2,0]
