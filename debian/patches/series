# You must remove unused comment lines for the released package.
#
# The patch files of the -p1 format included in the debian/patches directory
# are applied to the upstream source in the order listed below.
#
# This may be manually managed by users with dquilt (quilt(1) wrapper),
# git-buildpackage, etc.
#
# Also this may be updated by dpkg-source(1) when making a package.
#
# If dgit-maint-merge(7) workflow is deployed, let dgit to manage this.
