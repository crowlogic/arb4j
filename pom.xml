<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.arblib</groupId>
	<artifactId>arb4j</artifactId>
	<version>1.1.3</version>
	<packaging>jar</packaging>
	<description>arb4j</description>

	<properties>
		<maven.compiler.source>25</maven.compiler.source>
		<maven.compiler.target>25</maven.compiler.target>
		<maven.compiler.release>25</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<javafx.version>26.0.1</javafx.version>
		<asm.version>9.9.1</asm.version>
		<mdep.outputFile>class.path</mdep.outputFile>
		<argLine></argLine>
		<!-- When true, surefire writes each test class's stdout/stderr to its own
		     target/surefire-reports/<Class>-output.txt instead of the console.
		     Default false keeps local runs printing to the terminal; CI sets it
		     to true so every test's console output survives as an uploadable
		     artifact even when the JVM is later killed by an OOM or a timeout. -->
		<arb4j.redirectTestOutputToFile>false</arb4j.redirectTestOutputToFile>
	</properties>

	<repositories>
		<repository>
			<id>central</id>
			<url>https://repo1.maven.org/maven2</url>
		</repository>
		<repository>
			<id>sonatype-releases</id>
			<url>https://oss.sonatype.org/content/repositories/releases</url>
		</repository>
		<repository>
			<id>oracle</id>
			<url>https://download.oracle.com/maven</url>
		</repository>
	</repositories>

	<!--
	  Publish target for `mvn deploy`. GitHub Packages hosts the released
	  arb4j jar (plus the attached -sources and -javadoc jars) as a Maven
	  repository at https://maven.pkg.github.com/crowlogic/arb4j. The `github`
	  id matches the server entry that actions/setup-java writes into
	  ~/.m2/settings.xml in .github/workflows/release.yml, where the username
	  is the workflow actor and the password is the run's GITHUB_TOKEN.
	  Downstream consumers add the same URL as a <repository> and authenticate
	  with a personal access token carrying the read:packages scope.
	-->
	<distributionManagement>
		<repository>
			<id>github</id>
			<name>GitHub crowlogic/arb4j Apache Maven Packages</name>
			<url>https://maven.pkg.github.com/crowlogic/arb4j</url>
		</repository>
		<!--
		  GitHub Packages serves releases and snapshots from the same URL, but
		  Maven routes -SNAPSHOT versions through <snapshotRepository>. Without
		  this entry `mvn deploy` of a SNAPSHOT aborts asking for an alt
		  snapshot deployment repository. Snapshots may be overwritten freely.
		-->
		<snapshotRepository>
			<id>github</id>
			<name>GitHub crowlogic/arb4j Apache Maven Packages</name>
			<url>https://maven.pkg.github.com/crowlogic/arb4j</url>
		</snapshotRepository>
	</distributionManagement>

	<dependencies>
		<dependency>
			<groupId>org.vineflower</groupId>
			<artifactId>vineflower</artifactId>
			<version>1.11.2</version>
		</dependency>
		<dependency>
			<groupId>guru.nidi</groupId>
			<artifactId>graphviz-java</artifactId>
			<version>0.18.1</version>
		</dependency>

		<dependency>
			<groupId>org.yaml</groupId>
			<artifactId>snakeyaml</artifactId>
			<version>2.4</version>
		</dependency>
		<dependency>
			<groupId>org.ow2.asm</groupId>
			<artifactId>asm</artifactId>
			<version>${asm.version}</version>
		</dependency>
		<dependency>
			<groupId>org.ow2.asm</groupId>
			<artifactId>asm-commons</artifactId>
			<version>${asm.version}</version>
		</dependency>
		<dependency>
			<groupId>org.ow2.asm</groupId>
			<artifactId>asm-util</artifactId>
			<version>${asm.version}</version>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-base</artifactId>
			<version>${javafx.version}</version>
			<classifier>linux</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-swing</artifactId>
			<version>${javafx.version}</version>
			<classifier>linux</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-graphics</artifactId>
			<version>${javafx.version}</version>
			<classifier>linux</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-controls</artifactId>
			<version>${javafx.version}</version>
			<classifier>linux</classifier>
			<exclusions>
				<exclusion>
					<groupId>org.openjfx</groupId>
					<artifactId>javafx-controls</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.scilab.forge</groupId>
			<artifactId>jlatexmath</artifactId>
			<version>1.0.7</version>
		</dependency>
		<dependency>
			<groupId>io.fair-acc</groupId>
			<artifactId>chartfx</artifactId>
			<version>11.3.1</version>
			<exclusions>
				<exclusion>
					<groupId>org.openjfx</groupId>
					<artifactId>javafx-controls</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.openjfx</groupId>
					<artifactId>javafx-base</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.openjfx</groupId>
					<artifactId>javafx-graphics</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.13.2</version>
		</dependency>
		<dependency>
			<groupId>guru.nidi.com.kitfox</groupId>
			<artifactId>svgSalamander</artifactId>
			<version>1.1.3</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>2.0.9</version>
		</dependency>

		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.4.14</version>
		</dependency>

		<dependency>
			<groupId>info.picocli</groupId>
			<artifactId>picocli</artifactId>
			<version>4.7.6</version>
		</dependency>

	</dependencies>

	<build>
		<directory>build</directory>
		<outputDirectory>build/classes</outputDirectory>
		<testOutputDirectory>build/test-classes</testOutputDirectory>
		<resources>
			<!--
			  Standard resource processing: everything under src/main/resources
			  is packaged into the jar preserving its layout. The native library
			  src/main/resources/native/libarblib.so therefore lands at
			  native/libarblib.so in the jar (the path NativeLibraryLoader
			  extracts), and css/png/logback are packaged too. The repo-root
			  libarblib.so is a symlink to this same file, so the dev
			  -Djava.library.path=. binary and the packaged binary are identical.
			-->
			<resource>
				<directory>src/main/resources</directory>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.14.0</version>
				<configuration>
					<release>25</release>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
<argLine>-ea --illegal-native-access=allow
						--sun-misc-unsafe-memory-access=allow
						-Djava.library.path=${project.basedir}
						-XX:+DisableExplicitGC
						${argLine}</argLine>
					<redirectTestOutputToFile>${arb4j.redirectTestOutputToFile}</redirectTestOutputToFile>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
				<configuration>
					<archive>
						<manifest>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.6.3</version>
				<configuration>
					<source>25</source>
					<additionalJOptions>
						<additionalJOption>-Xdoclint:none</additionalJOption>
						<additionalJOption>-quiet</additionalJOption>
					</additionalJOptions>
					<!--
					  maven-javadoc-plugin's default package enumeration walks
					  the source tree and emits a flat @packages file to the
					  javadoc tool. javadoc 25 then aborts with

					      error: No source files for package <name>

					  whenever any listed package contains only
					  package-info.java (the package directory has no
					  ordinary .java sources).

					  Switching from <packages> to <subpackages> tells javadoc
					  to discover sources recursively under <sourcepath>
					  rather than processing an explicit list. Under that
					  mode a directory whose only file is package-info.java
					  is silently skipped instead of failing the run.
					-->
					<sourcepath>${project.basedir}/src/main/java</sourcepath>
					<subpackages>arb</subpackages>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.3.0</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.3.1</version>
				<configuration>
					<outputDirectory>build/classes</outputDirectory>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>3.6.1</version>
				<executions>
					<!--
						Development classpath: paths point into the local ~/.m2
						repository so a checkout's bin/* wrappers can resolve
						dependencies straight out of Maven's cache. Bound to
						generate-sources so it is in place before any compile.
					-->
					<execution>
						<id>build-classpath</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>build-classpath</goal>
						</goals>
						<configuration>
							<outputFile>${project.basedir}/class.path</outputFile>
							<includeScope>runtime</includeScope>
						</configuration>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

	<profiles>

		<!--
		  Tracing profile: enable expression-compiler debug logging plus the
		  static `arb4j.trace` and `arb4j.traceNodes` flags so a single
		  invocation of `mvn -Ptrace test -Dtest=Foo#bar`
		-->
		<profile>
			<id>trace</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<argLine>-ea --illegal-native-access=allow
								--sun-misc-unsafe-memory-access=allow
								-Djava.library.path=${project.basedir}
								-Darb4j.trace=true
								-Darb4j.traceNodes=true
								-Darb4j.saveClasses=true
								-Darb4j.decompileClasses=true
								-Dlogback.configurationFile=logback-debug.xml</argLine>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!--
		  Native memory leak detection profile. Runs only
		  MuntzPadeApproximantAbandonedAllocationTest, which requires
		  explicit System.gc() calls to trigger LeakTracker queue
		  processing. The main test run disables explicit GC via
		  -XX:+DisableExplicitGC to avoid 10 unnecessary Full GC pauses
		  (~160ms total).
		  
		  Usage: mvn -Pleak-test test
		-->
		<profile>
			<id>leak-test</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<argLine>-ea --illegal-native-access=allow
								--sun-misc-unsafe-memory-access=allow
								-Djava.library.path=${project.basedir}
								${argLine}</argLine>
							<includes>
								<include>**/MuntzPadeApproximantAbandonedAllocationTest.java</include>
							</includes>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
