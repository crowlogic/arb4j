package arb;

public class PartitionedFloatInterval 
{
  FloatInterval interval;
  Float partitions;
  
  public PartitionedFloatInterval( FloatInterval interval, int n )
  {
    assert false : "allocate a chunk of memory and then use Cptr offsets for fast access";   
  }
}
