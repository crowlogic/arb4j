/**
 * Fast and lightweight expression evaluator. It is also intended packaging
 * classes for compiling String literal expressions into Java bytecodes for
 * evaluation by the Java virtual machine rather than rather than interpreting
 * some parsed data structure upone each iteration.
 */
package arb.algebraic.expressions;