package arb;

public class FloatIntervalPartition 
{
  FloatInterval interval;
  Float partitions;
  
  public FloatIntervalPartition( FloatInterval interval, int n )
  {
    assert false : "allocate a chunk of memory and then use Cptr offsets for fast access";   
  }
}
