/**
 * The components generated by parsing {@link java.lang.String}s containing the
 * representation of the {@link arb.expressions.Expression} to be compiled into a
 * Java class, which can then be instantiated and interpreted by the JVM or
 * turned into native code with Graal, or anythig other thing you can do with
 * class files is located in this package. How's that for a run-on sentence?
 */
package arb.expressions.nodes;