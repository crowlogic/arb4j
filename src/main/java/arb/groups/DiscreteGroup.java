package arb.groups;

/**
 * A group G is a discrete group if it contains no limit points. (i.e., for each
 * element in G, there is a neighborhood which only contains that element).
 * Equivalently, the group G is discrete if and only if its identity is isolated
 * 
 */
public interface DiscreteGroup extends
                               Group
{

}
