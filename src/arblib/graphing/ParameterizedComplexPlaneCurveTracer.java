package arblib.graphing;

import arblib.ComplexFunction;
import arblib.curves.*;

public class ParameterizedComplexPlaneCurveTracer
{
  /**
   * TODO: evaluate a {@link ComplexFunction} which specifies the coordinates of
   * (x(t),y(t)) for -Pi<t<Pi and trace the outline onto a {@link BufferedImage} so
   * it can be traced out and drawn with {@link ComplexFunctionPlotter}, for
   * instance, the {@link Lemniscate}
   */
}
